<IfModule mod_fastcgi.c>
	FastCgiConfig -maxProcesses 1 -processSlack 1
</IfModule>

<VirtualHost *:80>
	# The ServerName directive sets the request scheme, hostname and port that
	# the server uses to identify itself. This is used when creating
	# redirection URLs. In the context of virtual hosts, the ServerName
	# specifies what hostname must appear in the request's Host: header to
	# match this virtual host. For the default virtual host (this file) this
	# value is not decisive as it is used as a last resort host regardless.
	# However, you must set it for any further virtual host explicitly.
	ServerName www.smart-earn.com

	ServerAdmin webmaster@localhost
	DocumentRoot /usr/local/smartearn/www

	# Available loglevels: trace8, ..., trace1, debug, info, notice, warn,
	# error, crit, alert, emerg.
	# It is also possible to configure the loglevel for particular
	# modules, e.g.
	#LogLevel info ssl:warn

	ErrorLog ${APACHE_LOG_DIR}/error.log
	CustomLog ${APACHE_LOG_DIR}/access.log combined

	# For most configuration files from conf-available/, which are
	# enabled or disabled at a global level, it is possible to
	# include a line for only one particular virtual host. For example the
	# following line enables the CGI configuration for this host only
	# after it has been globally disabled with "a2disconf".
	#Include conf-available/serve-cgi-bin.conf

        FastCgiServer /usr/local/smartearn/www/sgweb -initial-env CPPCMS_CONFIG=/usr/local/smartearn/www/fcgiconfig.js -socket /tmp/sgweb-socket
        ScriptAliasMatch ^/(.*)$ /usr/local/smartearn/www$1
        AddHandler fastcgi-script /usr/local/smartearn/www/sgweb

        Alias / "/usr/local/smartearn/www/"
        <Directory /usr/local/smartearn/www/>
                Options         Indexes FollowSymLinks MultiViews
                AllowOverride   All
                Require all granted
        </Directory>


        RewriteEngine On
#        RewriteRule ^(/blob/.*)$ /usr/local/smartearn/sgweb/blob/$1 [PT]
        RewriteRule ^/(.+)$ /usr/local/smartearn/www/sgweb/$1 [QSA,L]
        RewriteRule ^/(.?)$ /usr/local/smartearn/www/sgweb/home [QSA,L]

        Alias /awstatsclasses "/usr/share/awstats/lib/"
        Alias /awstats-icon "/usr/share/awstats/icon/"
        Alias /awstatscss "/usr/share/doc/awstats/examples/css"
        ScriptAlias /awstats/ /usr/lib/cgi-bin/
        Options ExecCGI MultiViews SymLinksIfOwnerMatch


</VirtualHost>

#<VirtualHost *:81>
#	DocumentRoot	/usr/local/smartearn/w3admin
#	FastCgiServer /usr/local/smartearn/w3admin/sgweb -initial-env CPPCMS_CONFIG=/usr/local/smartearn/w3admin/fcgiconfig.js -socket /tmp/sgwebadm-socket
#	ScriptAliasMatch ^/(.*)$ /usr/local/smartearn/w3admin$1
#	AddHandler fastcgi-script /usr/local/smartearn/w3admin/sgweb
#
#	Alias / "/usr/local/smartearn/w3admin/"
#	<Directory /usr/local/smartearn/w3admin/>
#		Options 	Indexes FollowSymLinks MultiViews
#		AllowOverride	All
#		Require all granted
#	</Directory>
#	RewriteEngine On
#</VirtualHost>

<VirtualHost *:80>
        ServerName      purebuy.in
        ServerAlias     www.purebuy.in
        DocumentRoot    /usr/local/smartearn/webtemp
        ServerAdmin     admin@smart-earn.com
        <Directory /usr/local/smartearn/webtemp>
                Options         Indexes FollowSymLinks
                AllowOverride   All
                Require all granted
        </Directory>
#        <Location />
#                RewriteEngine on
#                RewriteCond %{HTTPS} off 				[NC]
#                RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URL} 	[L,R]
#        </Location>
</VirtualHost>


#<VirtualHost *:443>
#        ServerAdmin     admin@smart-earn.com
#        DocumentRoot    /usr/local/smartearn/webtemp
#        ServerName      smart-earn.com
#	ServerAlias	*.smart-earn.com
#        SSLEngine       on
#        SSLCertificateFile      /etc/ssl/certs/smart-earn_com.crt
#        SSLCertificateKeyFile   /etc/ssl/private/smart-earn.key
#        SSLCertificateChainFile /etc/ssl/certs/smart-earn_com.ca-bundle
#        <Location />
#                SSLRequireSSL   on
#                SSLVerifyClient optional
#                SSLVerifyDepth  1
#                SSLOptions +StdEnvVars +StrictRequire
#        </Location>
#        <Directory /usr/local/smartearn/webtemp>
#                Require all granted
#        </Directory>
#</VirtualHost>

#<VirtualHost *:80>
#        ServerName      smartearn.co
#        ServerAlias     www.smartearn.co
#        DocumentRoot    /usr/local/smartearn/webtemp
#        ServerAdmin     admin@smart-earn.com
#        <Directory /usr/local/smartearn/webtemp>
#                Options         Indexes FollowSymLinks
#                AllowOverride   All
#                Require all granted
#        </Directory>
#        <Location />
#                RewriteEngine on
#                RewriteCond %{HTTPS} off                               [NC]
#                RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URL}    [L,R]
#        </Location>
#</VirtualHost>



<VirtualHost *:80>
        ServerName      smartgaze.co
        ServerAlias     www.smartgaze.co
        DocumentRoot    /usr/local/smartearn/webtemp
        ServerAdmin     admin@smart-earn.com
        <Directory /usr/local/smartearn/webtemp>
                Options         Indexes FollowSymLinks
                AllowOverride   All
                Require all granted
        </Directory>
#        <Location />
#                RewriteEngine on
#                RewriteCond %{HTTPS} off                               [NC]
#                RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URL}    [L,R]
#        </Location>
</VirtualHost>

<VirtualHost *:80>
        ServerName      smartgaze.in
        ServerAlias     www.smartgaze.in
        DocumentRoot    /usr/local/smartearn/webtemp
        ServerAdmin     admin@smart-earn.com
        <Directory /usr/local/smartearn/webtemp>
                Options         Indexes FollowSymLinks
                AllowOverride   All
                Require all granted
        </Directory>
#        <Location />
#                RewriteEngine on
#                RewriteCond %{HTTPS} off                               [NC]
#                RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URL}    [L,R]
#        </Location>
</VirtualHost>

<VirtualHost *:80>
        ServerName      purebuy.co
        ServerAlias     www.purebuy.co
        DocumentRoot    /usr/local/smartearn/webtemp
        ServerAdmin     admin@smart-earn.com
        <Directory /usr/local/smartearn/webtemp>
                Options         Indexes FollowSymLinks
                AllowOverride   All
                Require all granted
        </Directory>
#        <Location />
#                RewriteEngine on
#                RewriteCond %{HTTPS} off                               [NC]
#                RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URL}    [L,R]
#        </Location>
</VirtualHost>

<VirtualHost *:80>
        ServerName      dtone.us.com
        ServerAlias     *.dtone.us.com
        DocumentRoot    /usr/local/smartearn/webtemp
        ServerAdmin     admin@smart-earn.com
        <Directory /usr/local/smartearn/webtemp>
                Options         Indexes FollowSymLinks
                AllowOverride   All
                Require all granted
        </Directory>
#        <Location />
#                RewriteEngine on
#                RewriteCond %{HTTPS} off                               [NC]
#                RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URL}    [L,R]
#        </Location>
</VirtualHost>
